# 🎨 Frontend - Console Rental UI

Современный, красивый React интерфейс для системы аренды игровых консолей с тёмной темой, real-time обновлениями и Telegram интеграцией.

---

## 📋 Быстрый старт

```bash
# Установка зависимостей
npm install

# Запуск разработки
npm start

# Запуск production build
npm run build
```

**Приложение доступно на**: `http://localhost:3000`

---

## 🛠️ Технический стек

- **React** 18+ - UI библиотека
- **React Router** v6 - Маршрутизация
- **Axios** - HTTP клиент
- **React Icons** - Набор иконок
- **CSS3** - Стилизация (переменные CSS, градиенты, flex)

---

## 🎨 Дизайн & Тема

### Цветовая схема (тёмная тема)

```css
--bg-primary: #1a1a1a         /* Основной фон */
--bg-secondary: #0f0f0f       /* Вторичный фон */
--accent: #00d4ff             /* Голубой акцент */
--text-primary: #e0e0e0       /* Основной текст */
--text-secondary: #a0a0a0     /* Вторичный текст */
--error: #ff4444              /* Ошибка */
--success: #44ff44            /* Успех */
--border: #333333             /* Граница */
```

### Отзывчивость

- **Desktop**: 1920px+ (полная верстка)
- **Tablet**: 768px - 1919px (адаптированная)
- **Mobile**: < 768px (мобильная версия)

---

## 📁 Структура проекта

```
frontend/
├── src/
│   ├── components/           # Переиспользуемые компоненты
│   │   ├── ConfirmDialog.js
│   │   ├── ConfirmDialog.css
│   │   ├── Navigation.js
│   │   ├── Navigation.css
│   │   ├── NotificationCenter.js
│   │   ├── NotificationCenter.css
│   │   ├── RatingModal.js
│   │   ├── RatingModal.css
│   │   └── Toast.js
│   │
│   ├── pages/                # Страницы приложения
│   │   ├── Dashboard.js
│   │   ├── Dashboard.css
│   │   ├── ConsolesPage.js
│   │   ├── ConsolesPage.css
│   │   ├── GamesPage.js
│   │   ├── GamesPage.css
│   │   ├── DiscountsPage.js
│   │   ├── DiscountsPage.css
│   │   ├── RatingsPage.js
│   │   ├── RatingsPage.css
│   │   ├── RentalsPage.js
│   │   ├── RentalsPage.css
│   │   ├── UsersPage.js
│   │   ├── UsersPage.css
│   │   └── HistoryPage.js
│   │
│   ├── styles/               # Общие стили
│   │   └── ...
│   │
│   ├── App.js                # Корневой компонент
│   ├── App.css
│   ├── index.js              # Точка входа
│   └── index.css
│
├── public/
│   └── index.html            # HTML шаблон
│
├── package.json
├── .env
└── README.md
```

---

## 📄 Компоненты

### Navigation.js - Навигация

**Функции:**
- Боковое меню с ссылками на все страницы
- Иконка уведомлений с количеством ожидающих рейтингов
- Логотип приложения
- Адаптивное меню для мобильных

**Маршруты:**
```
🏠 Dashboard      - Главная страница
🎮 Консоли       - Управление консолями
🎮 Игры          - Управление играми
📋 Аренды        - История аренд
🏷️ Скидки        - Управление скидками
👥 Пользователи  - Список пользователей
⭐ Рейтинги      - Модерация рейтингов
📜 История       - История операций
```

---

### Toast.js - Уведомления

**Использование:**
```javascript
import { addToast } from './Toast';

addToast('Успешно!', 'success');      // 🟢 Зелёный
addToast('Ошибка!', 'error');        // 🔴 Красный
addToast('Информация', 'info');      // 🔵 Синий
addToast('Предупреждение', 'warn');  // 🟡 Жёлтый
```

**Параметры:**
- Расположение: нижний правый угол
- Автоскрытие: 3 секунды
- Стекируемые уведомления

---

### ConfirmDialog.js - Подтверждение

**Использование:**
```javascript
const [showConfirm, setShowConfirm] = useState(false);

const handleDelete = () => {
  setShowConfirm(true);  // Показать диалог
};

<ConfirmDialog
  title="Удалить?"
  message="Это действие нельзя отменить"
  onConfirm={async () => {
    await axios.delete(`/api/consoles/${id}`);
    setShowConfirm(false);
  }}
  onCancel={() => setShowConfirm(false)}
/>
```

---

### RatingModal.js - Оценивание

**Функции:**
- Выбор рейтинга (1-5 звёзд)
- Выбор категории (speed/quality/communication/overall)
- Написание комментария (макс 500 символов)
- Отправка на модерацию

**Props:**
```javascript
<RatingModal
  rental={rentalObject}
  currentUserId={userId}
  onClose={() => setShowModal(false)}
/>
```

---

### NotificationCenter.js - Центр уведомлений

**Функции:**
- Показывает количество ожидающих рейтингов
- Выпадающее меню со списком
- Auto-refresh каждые 30 секунд
- Красный значок с числом

---

## 📄 Страницы

### 🏠 Dashboard - Главная

**Real-time метрики:**
- 📊 Всего консолей
- 🎮 Активных аренд
- 👥 Зарегистрированных пользователей
- ⭐ Ожидающих рейтингов
- 💰 Общая прибыль

**Графики:**
- Активные аренды по дням
- Популярные консоли
- Топ пользователей

---

### 🎮 ConsolesPage - Консоли

**Функции:**
- Просмотр списка консолей
- Добавление новой консоли
- Редактирование информации
- Удаление консоли (с подтверждением)
- Фильтрация по типу
- Поиск по названию

**Поля:** name, type, model, description, pricePerDay, games, isAvailable

---

### 🎮 GamesPage - Игры

**Функции:**
- Список всех игр
- Добавление новой игры
- Редактирование информации
- Удаление игры
- Привязка к консолям

**Поля:** name, description, genre, year, platform

---

### 📋 RentalsPage - Аренды

**Функции:**
- Просмотр всех аренд
- Фильтрация: активные/завершённые/все
- Создание новой аренды
- Завершение аренды
- Оценивание завершённой аренды (⭐ кнопка)
- Калькулятор цены со скидками

**Статусы:** active, completed, cancelled

---

### 🏷️ DiscountsPage - Скидки

**Функции:**
- Просмотр скидок по месяцам
- Интерактивный календарь
- Клик на дату = выбор скидки
- Добавление новой скидки
- Редактирование скидки
- Удаление скидки
- Глобальные скидки

**Пример:** Кликните на 14 февраля → скидка 25%

---

### ⭐ RatingsPage - Рейтинги

**Админ-панель модерации:**
- Просмотр ожидающих рейтингов
- Одобрение рейтинга → Telegram уведомление
- Отклонение рейтинга → форма причины
- Удаление рейтинга
- Просмотр звёзд и комментариев
- Фильтрация по статусу

**При одобрении:**
1. Рейтинг получает статус "approved"
2. User.averageRating пересчитывается
3. Отправляется Telegram уведомление пользователю

---

### 👥 UsersPage - Пользователи

**Функции:**
- Список всех пользователей
- Фото из Telegram
- Средний рейтинг каждого
- Количество рейтингов
- Telegram ID и юзернейм
- Редактирование профиля
- Удаление пользователя

**Отображение:** ⭐ 4.5 (12 оценок)

---

### 📜 HistoryPage - История

**Функции:**
- Просмотр всех операций (create/update/delete)
- Фильтрация по типу (console/rental/user/discount/rating/game)
- Показ старых и новых данных
- Откат операции (undo)
- Временная метка каждой операции

---

## 🔄 State Management

### Context для Toast

```javascript
// В App.js используется для глобальных уведомлений
// Позволяет вызывать addToast из любого места приложения
```

### Local State

```javascript
// Каждая страница управляет своим состоянием:
const [data, setData] = useState([]);
const [loading, setLoading] = useState(false);
const [error, setError] = useState(null);
```

### API Calls

```javascript
// Axios for HTTP requests
import axios from 'axios';

const fetchData = async () => {
  try {
    const response = await axios.get('/api/endpoint');
    setData(response.data);
  } catch (error) {
    addToast('Ошибка загрузки', 'error');
  }
};
```

---

## 🎯 Ключевые функции

### ✅ Real-time Dashboard
- Автоматическое обновление метрик
- Графики и статистика
- Быстрые действия

### ✅ Система Рейтингов
- ⭐ Оценивание после аренды
- Модерация администратором
- Telegram уведомления
- Средняя оценка пользователя

### ✅ Скидки с Календарём
- Интерактивный календарь
- Кликабельные даты
- Визуальное отображение скидок

### ✅ История Операций
- Полный аудит всех действий
- Откат (undo) операций
- Сохранение старых/новых значений

### ✅ Уведомления
- Toast notifications (нижний правый угол)
- Notification Center (иконка с бейджем)
- Telegram Bot уведомления
- Auto-dismiss через 3 сек

---

## 🌐 API Интеграция

### Конфигурация

```env
# .env
REACT_APP_API_URL=http://localhost:5000
NODE_ENV=development
```

### Примеры запросов

```javascript
// GET - Получить все консоли
const consoles = await axios.get('/api/consoles');

// POST - Создать консоль
await axios.post('/api/consoles', {
  name: 'Xbox Series X',
  pricePerDay: 600
});

// PUT - Обновить консоль
await axios.put('/api/consoles/id', { name: 'Updated' });

// DELETE - Удалить консоль
await axios.delete('/api/consoles/id');
```

---

## 🎨 Кастомизация CSS

### Изменение цветов

**Файл:** `src/index.css`

```css
:root {
  --bg-primary: #1a1a1a;
  --accent: #00d4ff;
  /* ... другие переменные */
}

/* Применение: */
background-color: var(--bg-primary);
color: var(--accent);
```

### Media Queries

```css
@media (max-width: 768px) {
  .sidebar { display: none; }
  .container { padding: 10px; }
}
```

---

## 🚀 npm Команды

```bash
npm start                    # Разработка (порт 3000)
npm run build               # Production build
npm test                    # Запустить тесты
npm run eject               # Eject конфигурацию
```

---

## 🐛 Решение проблем

### Белый экран при запуске

```bash
npm cache clean --force
rm -rf node_modules
npm install
npm start
```

### API не отвечает

```bash
# 1. Проверьте backend
curl http://localhost:5000/health

# 2. Проверьте .env
cat .env

# 3. Смотрите браузерную консоль (F12)
```

### Компонент не обновляется

```javascript
// Используйте useEffect
useEffect(() => {
  fetchData();
}, [id]);

// Используйте useCallback
const handleClick = useCallback(() => {
  // ...
}, [dependencies]);
```

---

## 📱 Мобильная оптимизация

✅ Адаптивное меню  
✅ Мобильные компоненты  
✅ Touch-friendly кнопки  
✅ Оптимизированные шрифты  

---

## 🔐 Безопасность

✅ Валидация входных данных  
✅ Обработка ошибок API  
✅ Защита от XSS (React автоматически)  
✅ Sanitizing HTML контента  

---

**Версия**: 1.0.0  
**Последнее обновление**: Январь 2025  
**Статус**: ✅ Готова к использованию

## Функции

### 📊 Главная панель
- Статистика консолей
- Активные аренды
- Доход
- Количество пользователей

### 🎮 Управление консолями
- Просмотр всех консолей
- Добавление новых консолей
- Удаление консолей
- Отслеживание статуса (Доступна/Арендована/Обслуживание)

### 👥 Управление пользователями
- Просмотр всех зарегистрированных пользователей
- Информация из Telegram
- История пользователя

### 📋 Управление арендами
- Просмотр активных и завершенных аренд
- Завершение аренды
- Отслеживание доходов

## Технологии

- React 18
- React Router v6
- Axios для API запросов
- React Icons для иконок
- CSS Grid/Flexbox для макета

## Структура проекта

```
frontend/
├── public/
│   └── index.html
├── src/
│   ├── components/
│   │   └── Navigation.js
│   ├── pages/
│   │   ├── Dashboard.js
│   │   ├── ConsolesPage.js
│   │   ├── UsersPage.js
│   │   └── RentalsPage.js
│   ├── styles/
│   │   ├── ConsolesPage.css
│   │   ├── UsersPage.css
│   │   ├── RentalsPage.css
│   │   └── Dashboard.css
│   ├── App.js
│   ├── App.css
│   ├── index.js
│   └── index.css
└── package.json
```
